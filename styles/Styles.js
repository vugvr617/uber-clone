import { StyleSheet } from 'react-native';

export const mainStyles = StyleSheet.create({
  views: {container: {
    flex: 1,
    backgroundColor: '#fff',
    width: '100%',
    alignItems: 'center',
  },
  main: {
    width: '90%',
    flex: 1,
  }}
});

